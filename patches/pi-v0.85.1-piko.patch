diff --git a/packages/agent/src/harness/agent-harness.ts b/packages/agent/src/harness/agent-harness.ts
index 55b853e..2a9bf08 100644
--- a/packages/agent/src/harness/agent-harness.ts
+++ b/packages/agent/src/harness/agent-harness.ts
@@ -447,6 +447,8 @@ export interface HookMap {
 	before_request: {
 		event: {
 			model: Model<Api>;
+			/** Durable Pi step identity used by adapters to fence provider attempts. */
+			stepId: string;
 			step: "assistant" | "deferred" | "compaction" | "branch_summary";
 			attempt: number;
 			streamOptions: AgentHarnessStreamOptions;
@@ -533,6 +535,8 @@ export interface AgentHarnessOptions<TContext extends object | undefined = objec
 	toolExecution?: "sequential" | "parallel";
 	toProviderMessages?: (messages: AgentMessage[], context: Context) => Message[] | Promise<Message[]>;
 	entryProjectors?: Record<string, EntryProjector>;
+	/** Process-local additive observer for provider-native usage before normalization. */
+	onRawUsage?: (usage: unknown, context: Context) => void;
 }
 
 export interface AgentLane {
diff --git a/packages/agent/src/harness/execution/assistant.ts b/packages/agent/src/harness/execution/assistant.ts
index 23c58e4..0ca6996 100644
--- a/packages/agent/src/harness/execution/assistant.ts
+++ b/packages/agent/src/harness/execution/assistant.ts
@@ -54,6 +54,7 @@ export interface HarnessAssistantStreamConfig {
 		metadata: AssistantResponseMetadata,
 		context: Context,
 	) => Promise<SettledAssistantMessage>;
+	onRawUsage?: (usage: unknown, context: Context) => void;
 	request(
 		aiContext: AiContext,
 		options: SimpleStreamOptions,
@@ -87,6 +88,7 @@ function createRequestOptions(
 		onResponse: (response) => {
 			captureMetadata({ status: response.status, headers: response.headers });
 		},
+		onRawUsage: (usage) => config.onRawUsage?.(usage, context),
 	};
 }
 
diff --git a/packages/agent/src/harness/runtime/drive/deferred.ts b/packages/agent/src/harness/runtime/drive/deferred.ts
index 1a11a5f..98dffb3 100644
--- a/packages/agent/src/harness/runtime/drive/deferred.ts
+++ b/packages/agent/src/harness/runtime/drive/deferred.ts
@@ -107,6 +107,7 @@ async function prepareDeferredPoll<TContext extends object | undefined>(
 			lane: lane.name,
 			runId: drive.operationId,
 			model,
+			stepId: expected.stepId,
 			step: "deferred",
 			attempt: poll,
 			streamOptions: baseOptions,
diff --git a/packages/agent/src/harness/runtime/drive/generation.ts b/packages/agent/src/harness/runtime/drive/generation.ts
index 15d305e..ff70aa6 100644
--- a/packages/agent/src/harness/runtime/drive/generation.ts
+++ b/packages/agent/src/harness/runtime/drive/generation.ts
@@ -107,6 +107,7 @@ async function prepareGeneration<TContext extends object | undefined>(
 			lane: lane.name,
 			runId: drive.operationId,
 			model,
+			stepId: generation.generationContext.stepId,
 			step: "assistant",
 			attempt: generation.nextAttempt,
 			streamOptions: generation.generationContext.streamOptions,
@@ -210,6 +211,7 @@ async function performGeneration<TContext extends object | undefined>(
 					return result?.payload;
 				},
 				afterResponse: response.afterResponse,
+				onRawUsage: (usage, context) => lane.readConfig().onRawUsage?.(usage, context),
 				request: (aiContext, options, context) => {
 					const admitted = withAbortSignal(drive.gate.signal, context);
 					return drive.gate.admit(() =>
diff --git a/packages/agent/src/harness/runtime/drive/structural.ts b/packages/agent/src/harness/runtime/drive/structural.ts
index ea68781..1bb93e4 100644
--- a/packages/agent/src/harness/runtime/drive/structural.ts
+++ b/packages/agent/src/harness/runtime/drive/structural.ts
@@ -815,6 +815,7 @@ async function performStructuralAttempt<TContext extends object | undefined>(
 					lane: lane.name,
 					runId: drive.operationId,
 					model,
+					stepId: effect.task.taskId,
 					step: summaryKind(effect.task),
 					attempt: effect.attempt,
 					streamOptions: baseOptions,
diff --git a/packages/agent/src/harness/runtime/harness.ts b/packages/agent/src/harness/runtime/harness.ts
index 633b316..908e3fd 100644
--- a/packages/agent/src/harness/runtime/harness.ts
+++ b/packages/agent/src/harness/runtime/harness.ts
@@ -70,6 +70,7 @@ export class Harness<TContext extends object | undefined> implements AgentHarnes
 				systemPrompt: options.systemPrompt,
 				toProviderMessages: options.toProviderMessages ?? ((messages) => convertToLlm(messages)),
 				entryProjectors: options.entryProjectors ?? {},
+				onRawUsage: options.onRawUsage,
 			},
 		};
 		for (const [name, state] of restored) this.lanesByName.set(name, this.buildLane(name, state));
diff --git a/packages/agent/src/harness/runtime/types.ts b/packages/agent/src/harness/runtime/types.ts
index 006ac12..7413b8c 100644
--- a/packages/agent/src/harness/runtime/types.ts
+++ b/packages/agent/src/harness/runtime/types.ts
@@ -36,6 +36,7 @@ export interface Config<TContext extends object | undefined> {
 	readonly systemPrompt: AgentHarnessOptions<TContext>["systemPrompt"];
 	readonly toProviderMessages: NonNullable<AgentHarnessOptions<TContext>["toProviderMessages"]>;
 	readonly entryProjectors: Readonly<NonNullable<AgentHarnessOptions<TContext>["entryProjectors"]>>;
+	readonly onRawUsage: AgentHarnessOptions<TContext>["onRawUsage"];
 }
 
 /** The current durable state owned by one lane. */
diff --git a/packages/ai/src/api/openai-codex-responses.ts b/packages/ai/src/api/openai-codex-responses.ts
index d7e3749..a0c25e7 100644
--- a/packages/ai/src/api/openai-codex-responses.ts
+++ b/packages/ai/src/api/openai-codex-responses.ts
@@ -392,7 +392,7 @@ export const stream: StreamFunction<"openai-codex-responses", OpenAICodexRespons
 						response = await (options?.fetch ?? globalThis.fetch)(resolveCodexUrl(model.baseUrl), {
 							method: "POST",
 							headers: sseHeaders,
-							body: sseBody,
+							body: sseBody as BodyInit,
 							signal: combinedSignal.signal,
 						});
 					} catch (error) {
diff --git a/packages/ai/src/api/openai-responses-shared.ts b/packages/ai/src/api/openai-responses-shared.ts
index 7ac1add..6a540c6 100644
--- a/packages/ai/src/api/openai-responses-shared.ts
+++ b/packages/ai/src/api/openai-responses-shared.ts
@@ -104,6 +104,7 @@ function convertToolResultOutput<TApi extends Api>(
 }
 
 export interface OpenAIResponsesStreamOptions {
+	onRawUsage?: (usage: unknown) => void;
 	serviceTier?: ResponseCreateParamsStreaming["service_tier"];
 	grammarToolInputProperties?: ReadonlyMap<string, string>;
 	resolveServiceTier?: (
@@ -557,6 +558,7 @@ export async function processResponsesStream<TApi extends Api>(
 			output.responseId = response.id;
 		}
 		if (response?.usage) {
+			options?.onRawUsage?.(response.usage);
 			const inputDetails = response.usage.input_tokens_details as
 				| { cached_tokens?: number; cache_write_tokens?: number }
 				| undefined;
diff --git a/packages/ai/src/api/openai-responses.ts b/packages/ai/src/api/openai-responses.ts
index 6855891..1a86937 100644
--- a/packages/ai/src/api/openai-responses.ts
+++ b/packages/ai/src/api/openai-responses.ts
@@ -173,6 +173,7 @@ export const stream: StreamFunction<"openai-responses", OpenAIResponsesOptions>
 			stream.push({ type: "start", partial: output });
 
 			await processResponsesStream(openaiStream, output, stream, model, {
+				onRawUsage: (usage) => options?.onRawUsage?.(usage, model),
 				serviceTier: options?.serviceTier,
 				grammarToolInputProperties,
 				applyServiceTierPricing: (usage, serviceTier) => applyServiceTierPricing(usage, serviceTier, model),
diff --git a/packages/ai/src/api/simple-options.ts b/packages/ai/src/api/simple-options.ts
index 2a30265..2781616 100644
--- a/packages/ai/src/api/simple-options.ts
+++ b/packages/ai/src/api/simple-options.ts
@@ -42,6 +42,7 @@ export function buildBaseOptions(
 		headers: options?.headers,
 		onPayload: options?.onPayload,
 		onResponse: options?.onResponse,
+		onRawUsage: options?.onRawUsage,
 		timeoutMs: options?.timeoutMs,
 		websocketConnectTimeoutMs: options?.websocketConnectTimeoutMs,
 		maxRetries: options?.maxRetries,
diff --git a/packages/ai/src/types.ts b/packages/ai/src/types.ts
index fe318fe..393dcc2 100644
--- a/packages/ai/src/types.ts
+++ b/packages/ai/src/types.ts
@@ -182,6 +182,8 @@ export interface StreamOptions extends ProviderRequestOptions<Model<Api>> {
 	 * its body stream is consumed.
 	 */
 	onResponse?: (response: ProviderResponse, model: Model<Api>) => void | Promise<void>;
+	/** Additive observer for the provider-native terminal usage object before normalization. */
+	onRawUsage?: (usage: unknown, model: Model<Api>) => void;
 	temperature?: number;
 	/**
 	 * Arbitrary sampling parameters merged into the request body as-is, after the named request
